// Deliberately almost empty: PRINCIPLES #6 is "zero-config or it doesn't ship", so a
// fixture that needs plugins to make the library work would be proving the opposite of
// what it is here to prove. `base` only makes the build servable from a file server.
export default { base: "./" };
