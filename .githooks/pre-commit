#!/usr/bin/env bash
# Server-side required PR checks own the main-branch gate (PRINCIPLES.md amendment 2.0.0).
# This hook owns the local harness audit; neither layer replaces the other.
set -euo pipefail
exec scripts/audit/harness-audit.sh
